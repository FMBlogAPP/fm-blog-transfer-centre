name: Update transfers

on:
  workflow_dispatch:
  push:
    paths:
      - "scripts/enrich_transfer_feed.py"
      - "scripts/apply_v21_embed_patch.py"
      - "patches/transfer-centre-v21.html"
      - ".github/workflows/update-transfers.yml"
  schedule:
    - cron: "17 */4 * * *"

permissions:
  contents: write

concurrency:
  group: transfer-updater
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Apply V2.1 Blogger UI
        run: python scripts/apply_v21_embed_patch.py

      - name: Fetch latest transfers
        if: github.event_name != 'push'
        env:
          API_FOOTBALL_KEY: ${{ secrets.API_FOOTBALL_KEY }}
        run: python scripts/update_transfers.py

      - name: Remove duplicate transfer records
        run: python scripts/cleanup_duplicates.py

      - name: Build V2.1 fast feed
        run: python scripts/enrich_transfer_feed.py

      - name: Commit changed data and embed
        run: |
          git config user.name "fm-blog-transfer-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/ blogger-embed.html
          if git diff --cached --quiet; then
            echo "No data or embed changes."
          else
            git commit -m "chore: refresh transfer centre"
            git fetch origin main
            git rebase origin/main
            git push
          fi
