name: Update FM TV

on:
  workflow_dispatch:
  push:
    paths:
      - "fm-tv/config.json"
      - "fm-tv/scripts/update_youtube.py"
  schedule:
    - cron: "23 * * * *"

permissions:
  contents: write

concurrency:
  group: fm-tv-updater
  cancel-in-progress: true

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Fetch latest YouTube videos
        env:
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
        run: python fm-tv/scripts/update_youtube.py

      - name: Commit changed FM TV data
        run: |
          git config user.name "fm-blog-fm-tv-bot"
          git config user.email "actions@users.noreply.github.com"
          git add fm-tv/data/videos.json
          if git diff --cached --quiet; then
            echo "No FM TV data changes."
          else
            git commit -m "chore: refresh FM TV videos"
            git pull --rebase origin main
            git push
          fi
